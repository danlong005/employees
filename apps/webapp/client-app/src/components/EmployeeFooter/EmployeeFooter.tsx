import React from 'react';
import { IEmployeeFooterProps } from '../../props';

export const EmployeeFooter: React.FC<IEmployeeFooterProps> = ({}: IEmployeeFooterProps) => {
  return (
    <div></div>
  );
}